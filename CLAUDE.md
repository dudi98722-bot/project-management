# הנחיות פרוייקט

## apartments-crm (אלכסנדר-דירות)

### כלל הרשאות — קבוע, נקבע ע"י דודי (2026-08-18)
**כל פיצ'ר חדש מוצג למנהלים (role admin) בלבד — גם בממשק וגם בנתונים שהשרת שולח.**
פתיחת פיצ'ר לעורכים/צופים/דמי-ניהול נעשית רק כשדודי מבקש זאת במפורש.
- בצד הלקוח: פריטי ניווט, לשוניות ומסכים חדשים נבדקים מול `CU.role==="admin"`.
- בצד השרת (`apartments-crm-AppsScript.gs`): טבלאות חדשות מרוקנות ב-`scopeDataForUser`
  למשתמש שאינו מנהל, ומשוחזרות מהשמור ב-`mergeSaveForUser` כדי ששמירה שלו לא תמחק אותן.
- נכון להיום מוסתרים ממי שאינו מנהל: לשונית שכירויות (rentals), גיליון חופשי (sheets).

### כללי עבודה שנלמדו בדם
- עריכת קבצים בסקריפט: לכתוב לקובץ זמני ואז `os.replace` — כתיבה ישירה שנכשלה
  באמצע כבר רוקנה את `apartments-crm.html` פעם אחת.
- שינויי `.gs` נכנסים לתוקף רק אחרי שדודי מדביק אותם ב-script.google.com
  (פריסה ← ניהול פריסות ← גרסה חדשה). לשלוח לו את הקובץ ולציין זאת בכל שינוי.
- פריסה ל-VPS: `ssh root@64.176.175.180` (מפתח ברירת מחדל) ומשיכת הקובץ מ-raw.githubusercontent
  עם ה-SHA של הקומיט (לא main — יש CDN cache).
- הודעות קומיט בעברית דרך קובץ (`git commit -F`) — here-string ב-PowerShell משבש עברית.
